# Student_management_system_C-_project
Student Management System built in C++ using STL. The program allows adding, updating, deleting, searching, and sorting students based on their grades and IDs. It also calculates total, average, and percentage for each student and supports ranking (top students).
